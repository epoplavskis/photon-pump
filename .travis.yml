sudo: required

language: python

python:
    - 3.6
    - 3.7
    - 3.5

services:
  - docker

before_install:
  - docker run -d -p 127.0.0.1:1113:1113 eventstore/eventstore

install: make

script:
  - pytest --cov =./
  - flake8 --exit-zero

after_success:
  - codecov
